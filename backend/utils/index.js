const { dinothunder } = require('./power-rangers-dt');
const { lightspeed } = require('./power-rangers-lr');
const { ninjastorm } = require('./power-rangers-ns');
const { rpm } = require('./power-rangers-rpm');
const { spd } = require('./power-rangers-spd');
const { timeforce } = require('./power-rangers-tf');
const { wildforce } = require('./power-rangers-wf');

const allRangers = [
    dinothunder,
    lightspeed,
    ninjastorm,
    rpm,
    spd,
    timeforce,
    wildforce
];

module.exports = {
    allRangers
}
